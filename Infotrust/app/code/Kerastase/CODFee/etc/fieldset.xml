<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:DataObject/etc/fieldset.xsd">
    <scope id="global">
        <!-- unfortunately doesn't work. bug? other workaround @see event: sales_model_service_quote_submit_before -->
        <fieldset id="sales_convert_quote">
            <field name="cod_fee">
                <aspect name="to_order" />
            </field>
            <field name="base_cod_fee">
                <aspect name="to_order" />
            </field>
        </fieldset>
        <fieldset id="sales_convert_quote_address">
            <field name="cod_fee">
                <aspect name="to_order" />
            </field>
            <field name="base_cod_fee">
                <aspect name="to_order" />
            </field>
        </fieldset>
    </scope>
</config>